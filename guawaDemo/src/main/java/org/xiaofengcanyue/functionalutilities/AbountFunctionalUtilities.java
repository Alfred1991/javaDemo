package org.xiaofengcanyue.functionalutilities;

/**
 * Guava提供两个基本functional接口：
 *   1、Function<A,B>只有一个方法 B apply(A input)
 *   2、Predicate<T>只有一个方法 boolean apply(T input)。
 */
public class AbountFunctionalUtilities {

    /**
     * Characters有一个特别的Predicate——CharMatcher。
     *
     * 此外，对于comparable types和comparison-based predicates，多数需求Range类型都可以满足。
     *
     */
}
